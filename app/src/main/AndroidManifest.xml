<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cggg.mx.edu.tesoem.itics.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".operativoActivity"></activity>
        <activity android:name=".BotaActivity" />
        <activity android:name=".BotasActivity" />
        <activity android:name=".sectorActivity" />
        <activity android:name=".PlacasActivity" />
        <activity android:name=".EsposasActivity" />
        <activity android:name=".UniformesActivity" />
        <activity android:name=".ArmasActivity" />
        <activity android:name=".patrullaActivity" />
        <activity android:name=".fRMpRINCIPALAactivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>